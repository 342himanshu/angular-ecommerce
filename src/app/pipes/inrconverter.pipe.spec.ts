import { INRConverterPipe } from './inrconverter.pipe';

describe('INRConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new INRConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
